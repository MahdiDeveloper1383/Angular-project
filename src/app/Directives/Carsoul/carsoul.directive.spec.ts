import { CarsoulDirective } from './carsoul.directive';

describe('CarsoulDirective', () => {
  it('should create an instance', () => {
    const directive = new CarsoulDirective();
    expect(directive).toBeTruthy();
  });
});
